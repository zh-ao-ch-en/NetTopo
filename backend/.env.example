# 数据库连接串。默认 SQLite（本地零演示）；相对路径会自动基于 backend 目录解析为绝对路径，避免启动位置不同造成漂移。
# 换 MySQL 时改为下面这行并安装 pymysql：
#   DATABASE_URL=mysql+pymysql://user:password@localhost:3306/lab
DATABASE_URL=sqlite:///./lab.db

# JWT 密钥（生产环境务必改成长随机串；复制本文件为 .env 后填写）
# 生成方式：openssl rand -hex 32  或  python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRE_MINUTES=1440

# 允许跨域的前端来源（逗号分隔）
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 登录限流：账号为主、IP 为辅（双层）。IP 层挡单源扫描，账号层防换 IP 爆破，admin 更严格。
# IP 层：同一 IP 连续失败 LOGIN_IP_MAX_FAILS 次后锁定 LOGIN_LOCK_MINUTES 分钟
# 账号层：同一账号连续失败 LOGIN_USER_MAX_FAILS 次后锁定 LOGIN_LOCK_MINUTES 分钟
# admin 特例：连续失败 LOGIN_ADMIN_MAX_FAILS 次后锁定 LOGIN_ADMIN_LOCK_MINUTES 分钟
LOGIN_IP_MAX_FAILS=5
LOGIN_USER_MAX_FAILS=5
LOGIN_ADMIN_MAX_FAILS=3
LOGIN_LOCK_MINUTES=15
LOGIN_ADMIN_LOCK_MINUTES=30